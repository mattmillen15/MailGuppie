# MailGuppie
Mailjet API tool for email verification and forced authentication. 

```python
python MailGuppie.py                          

╔════════════════════════════════════════════════╗
║                  MailGuppie                    ║
╚════════════════════════════════════════════════╝

          ><((((º>   ><((((º>   ><((((º>

usage: MailGuppie.py -t <targets.txt> -r <responder-ip> [options]

options:
  -h, --help            show this help message and exit
  -t, --targets <targets.txt>
                        Target email list file (.txt)
  -r, --responder <responder-ip>
                        Responder server IP/hostname
  -c, --config <template.conf>
                        Template config file (default: config/template.conf)
```
